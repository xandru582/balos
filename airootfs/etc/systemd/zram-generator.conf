# BalOS ZRAM — compressed RAM "swap" (much faster than disk swap)
# Gaming + battery win: no disk writes during swap

[zram0]
zram-size = min(ram, 16384)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
